import { StyleSheet } from "react-native";

export const InlineUserButtonsStyles = StyleSheet.create({
  container: {
    width: 30,
    height: 30,
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 15,
  },
  text: {
    fontSize: 12,
    fontWeight: "bold",
  },
});
